\chapter{Introduction}
\label{ch:intro}

\begin{theorem}[Main Theorem]\label{thm:main}
Let $(M^{n+1}, g)$ be a complete Riemannian manifold with non-empty smooth boundary $\partial M$, $2 \leq n \leq 6$.  Let $U \subset M$ be a \emph{good set} (Definition~\ref{def:good-set}), i.e., a bounded open set of finite perimeter satisfying $\mathbf{M}(\partial_I U) \leq \frac{1}{10}\, W_\partial(U)$, with $\overline{U} \cap \partial M \neq \varnothing$.  Then there exists a smooth, almost properly embedded free boundary minimal hypersurface $(\Sigma,\partial\Sigma)\subset(M,\partial M)$ with $\partial\Sigma\subset\partial M$ and $\Sigma$ meeting $\partial M$ orthogonally, satisfying
\[
\mathcal{H}^n(\Sigma)\le W_\partial(U)+\mathbf{M}(\partial_IU)
\qquad\text{and}\qquad
\Sigma\cap\overline{U}\neq\varnothing.
\]
\end{theorem}
\xinze{The min-max theory of Li--Zhou (Remark~1.3(a) of \cite{LZ17}) does not guarantee $\partial\Sigma \neq \varnothing$: the limit could be a closed minimal hypersurface. A possible modified statement:

\medskip
\noindent\textbf{Modified version.} Under the same hypotheses, there exists a smooth, almost properly embedded minimal hypersurface $\Sigma \subset M$ with $\Sigma \cap \overline{U} \neq \varnothing$ and $\mathcal{H}^n(\Sigma) \leq W_\partial(U) + \mathbf{M}(\partial_I U)$. Moreover, $\Sigma$ is either:
\begin{enumerate}
    \item a free boundary minimal hypersurface with $\partial\Sigma \subset \partial M$, $\Sigma$ meeting $\partial M$ orthogonally; or
    \item a closed (compact without boundary) embedded minimal hypersurface in the interior of $M$.
\end{enumerate}
Discuss with Yevgeny: can case (2) be ruled out by the good set construction or by geometric assumptions on $M$?}
